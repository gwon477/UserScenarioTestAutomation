# ScenarioForge Runtime System

The backend journal is canonical. Agents propose bounded work and staging artifacts through structured tools. Only backend validators and durable commits may advance lifecycle state.
