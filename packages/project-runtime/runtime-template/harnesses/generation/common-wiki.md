# Common WIKI Step

Purpose: write human-readable common workflow goals for the backend-owned reachable workflow skeleton. Do not classify business work and do not create scenarios.

Use only persisted FACT and workflow-skeleton receipts. Keep observations append-only within this session and do not restate prior context. Initial authoring submits one complete declared artifact. A correction submits only the workflow refs in the backend correction scope, echoes its base hash, and uses only the re-granted connected source ranges. A response, tool success, or agent end does not complete the step.
