# Deterministic Generation Step

This work is backend-owned and must not create an LLM session. It may read only the predecessor artifact receipts declared by its work descriptor and may write only its declared output artifact.
