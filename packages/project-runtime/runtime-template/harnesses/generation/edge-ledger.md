# Edge Ledger Step

Purpose: link the frozen FACT catalog into evidence-backed normal and exception edges only. Screen, element, API, predicate, workflow, and scenario structure are immutable inputs.

Use only exact opaque references and the bounded evidence supplied for this work. Keep observations append-only within this session and do not restate prior context. For initial authoring, submit one complete edge proposal. When the prompt contains `correction_plan`, submit only authorized `fact-correction-patch-v1.edge_changes` and omit every untargeted edge; the backend performs the merge. A response, tool success, or agent end does not complete the step.
